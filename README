To test:
 ~/google_appengine/dev_appserver.py -d .
  # -d = debug, -c = clear data first

To deploy:
 ~/google_appengine/appcfg.py update .

Console:
  http://localhost:8080/_ah/admin/datastore

Stuff added to README that I don't really want.
